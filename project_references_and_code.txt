======================================================
 AI INTERVIEW PLATFORM - PROJECT REFERENCES & CODE
======================================================

------------------------------------------------------
 1. IMPORTANT REFERENCES & DOCUMENTATION
------------------------------------------------------
- Google Gemini API Docs: https://ai.google.dev/docs
- Gemini Node.js SDK: https://www.npmjs.com/package/@google/generative-ai
- Gemini Python SDK: https://pypi.org/project/google-generativeai/
- React Docs: https://react.dev/
- Vite Docs: https://vitejs.dev/
- Tailwind CSS: https://tailwindcss.com/
- MongoDB Node Driver: https://www.mongodb.com/docs/drivers/node/current/

------------------------------------------------------
 2. PYTHON CODE (Jupyter Notebook / Testing)
------------------------------------------------------
# Install: pip install google-generativeai python-dotenv

import google.generativeai as genai
import os
from dotenv import load_dotenv

load_dotenv(dotenv_path='server/.env')
api_key = os.getenv('GEMINI_API_KEY')
genai.configure(api_key=api_key)

model = genai.GenerativeModel('gemini-1.5-flash')

prompt = """
You are an expert AI interviewer. 
Evaluate the following answer given by a candidate for a React Developer role.
Question: "Explain Virtual DOM"
Answer: "It's a copy of the real DOM that reacts faster to changes."
Provide a score out of 10 and brief feedback.
"""

response = model.generate_content(prompt)
print(response.text)

------------------------------------------------------
 3. NODE.JS / EXPRESS CODE (Backend Implementation)
------------------------------------------------------
// Install: npm install @google/generative-ai dotenv

const { GoogleGenerativeAI } = require("@google/generative-ai");
require("dotenv").config();

// Initialize the API with your API Key
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);

async function generateInterviewQuestion(role, difficulty) {
  try {
    const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });
    
    const prompt = `You are a technical interviewer. Generate 1 ${difficulty} level interview question for a ${role} position.`;
    
    const result = await model.generateContent(prompt);
    const response = await result.response;
    const text = response.text();
    
    console.log("Generated Question:", text);
    return text;
  } catch (error) {
    console.error("Error generating question:", error);
  }
}

// Call the function
// generateInterviewQuestion("Node.js Developer", "Medium");

------------------------------------------------------
 4. AI PROMPT ENGINEERING TEMPLATES
------------------------------------------------------

TEMPLATE 1: Generating Questions
"Act as an expert technical interviewer hiring for a {ROLE} position. Generate {COUNT} interview questions ranging from {DIFFICULTY} difficulty. The questions should test practical knowledge and problem-solving skills rather than just textbook definitions. Return the response in a JSON array format."

TEMPLATE 2: Evaluating Answers
"You are a strict but fair technical interviewer. Evaluate the candidate's answer based on accuracy, depth, and clarity.
Role: {ROLE}
Question: {QUESTION}
Candidate's Answer: {ANSWER}
Please provide:
1. A Score out of 10
2. Strengths of the answer
3. Areas for improvement
4. What the ideal answer should have included."

======================================================
